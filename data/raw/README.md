Store downloaded dataset archives here, such as `UOv2.zip`, `Brackish.zip`, and `RUOD.zip`.

This directory is intentionally excluded from Git because the archives are large and can be regenerated from their upstream sources.
